# Banner Editor

AI-assisted HTML5 campaign editor that adapts one master creative into controlled, editable banner formats.

## First MVP slice

- campaign format navigation;
- editable master canvas;
- element layers and inspector;
- adaptation locks;
- three-scene timeline;
- four target formats.

## Development

Run `npm install`, then `npm run dev`. Verify with `npm test` and `npm run build`.
