import {describe,expect,it} from "vitest";import {dimensionsFromFrameName,mappingLabel,matchFigmaFrames,type FigmaFrame} from "./figmaFrameMatching";import type {VisualFormat} from "./domain";
const formats=[{id:"a",size:"300×600",width:300,height:600},{id:"b",size:"300×250",width:300,height:250},{id:"c",size:"970×250",width:970,height:250},{id:"d",size:"320×50",width:320,height:50}] as VisualFormat[];
const frame=(id:string,name:string,width:number,height:number):FigmaFrame=>({id,name,width,height,type:"FRAME",pageId:"p",pageName:"Page"});
describe("Figma frame detection",()=>{it("reads actual frame dimensions and exact campaign match",()=>{const m=matchFigmaFrames([frame("1","Hero",300,600)],formats)[0];expect(m.exact[0].id).toBe("a");expect(mappingLabel(m)).toBe("exact")});it("uses frame name as secondary signal including META_300x600_V1",()=>{expect(dimensionsFromFrameName("META_300x600_V1")).toEqual({width:300,height:600});const m=matchFigmaFrames([frame("1","META_300x600_V1",301,601)],formats)[0];expect(m.exact).toHaveLength(0);expect(m.nameMatch[0].id).toBe("a");expect(mappingLabel(m)).toBe("name")});it("keeps duplicate-size frames independently selectable",()=>{const ms=matchFigmaFrames([frame("1","A",300,250),frame("2","B",300,250)],formats);expect(ms).toHaveLength(2);expect(ms.every(m=>m.duplicateSize)).toBe(true);expect(ms.every(m=>m.exact[0].id==="b")).toBe(true)});it("suggests nearest format but does not convert it to an exact/name match",()=>{const m=matchFigmaFrames([frame("1","Wide alt",960,240)],formats)[0];expect(m.exact).toHaveLength(0);expect(m.nameMatch).toHaveLength(0);expect(m.nearest).toBeTruthy();expect(mappingLabel(m)).toBe("nearest")})});