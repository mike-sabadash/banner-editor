import {useEffect,useMemo,useRef,useState,type PointerEvent as ReactPointerEvent} from "react";
import {ChevronRight,Copy,Crop,Eye,EyeOff,Grid3X3,Image as ImageIcon,Layers3,Magnet,MousePointer2,Pause,Play,Plus,Redo2,RectangleHorizontal,Sparkles,Type,Undo2,Upload} from "lucide-react";
import {api} from "../mvp2/api";
import type {Campaign} from "../mvp2/domain";
import {ttForFormat,validateScenesForTT} from "./campaignBridge";
import {DEFAULT_SCENES,EMPTY_RESPONSIVE_STATE,MASTER_FORMAT,RESPONSIVE_FAMILY_LABELS,affectedFormatsForFamily,formatInheritance,generateScene,normalizeResponsiveState,resolveSceneLayers,setLayerOverride,updateResponsiveMaster,useAsResponsiveMaster,type LayerOverride,type LayerRole,type LayoutFamily,type MotionPreset,type ResponsiveState,type Scene,type SceneLayer} from "./sceneModel";
import {FONT_STYLES,mergeCampaignFormats} from "./editorProductControls";
import "./sceneEditor.css";import "./campaignScene.css";import "./editorProductV2.css";import "./responsiveMasters.css";
const ROLES:LayerRole[]=["background","hero","logo","headline","copy","cta","legal","graphic"];
const PRESETS:{id:MotionPreset;label:string;glyph:string}[]=[{id:"none",label:"None",glyph:"•"},{id:"fade",label:"Fade",glyph:"◌"},{id:"from-left",label:"From left",glyph:"→"},{id:"from-right",label:"From right",glyph:"←"},{id:"from-top",label:"From top",glyph:"↓"},{id:"from-bottom",label:"From bottom",glyph:"↑"},{id:"scale-in",label:"Scale in",glyph:"↗"}];
const FONTS=["Inter","Manrope","Roboto","Montserrat","Golos Text","PT Sans","PT Serif","Rubik","Oswald","Unbounded"];
type Tool="select"|"text"|"image"|"shape";type SaveState="saved"|"saving"|"error";type ResizeHandle="nw"|"n"|"ne"|"e"|"se"|"s"|"sw"|"w";type Guides={x?:number;y?:number};type CropSnapshot={id:string;crop:{x:number;y:number;scale:number}};type MasterDialog={mode:"create"|"update";family:LayoutFamily}|null;type EditorSnapshot={scenes:Scene[];responsive:ResponsiveState};
const clamp=(v:number,min:number,max:number)=>Math.min(max,Math.max(min,v));
export default function WebSceneEditor({campaign}:{campaign?:Campaign}={}){
 const seeded=(campaign?.creativeDocument?.scenes as Scene[]|undefined)?.length?(campaign!.creativeDocument!.scenes as Scene[]):DEFAULT_SCENES;
 const [scenes,setScenes]=useState<Scene[]>(seeded),[sceneId,setSceneId]=useState(seeded[0].id),[layerId,setLayerId]=useState(seeded[0].layers[1]?.id||seeded[0].layers[0]?.id||""),[formatId,setFormatId]=useState("master"),[tool,setTool]=useState<Tool>("select"),[playing,setPlaying]=useState(false),[playMs,setPlayMs]=useState(0),[generated,setGenerated]=useState(false),[inspectorTab,setInspectorTab]=useState<"design"|"motion">("design"),[saveState,setSaveState]=useState<SaveState>("saved"),[snapEnabled,setSnapEnabled]=useState(true),[snapPx,setSnapPx]=useState(4),[guides,setGuides]=useState<Guides>({}),[editingTextId,setEditingTextId]=useState<string|null>(null),[cropEdit,setCropEdit]=useState<CropSnapshot|null>(null),[ttOpen,setTtOpen]=useState(false),[borderOn,setBorderOn]=useState(false),[borderColor,setBorderColor]=useState("#000000"),[spacePressed,setSpacePressed]=useState(false),[isPanning,setIsPanning]=useState(false);
 const [responsive,setResponsive]=useState<ResponsiveState>(()=>normalizeResponsiveState({responsiveMasters:campaign?.creativeDocument?.responsiveMasters||EMPTY_RESPONSIVE_STATE.responsiveMasters,formatOverrides:campaign?.creativeDocument?.formatOverrides||EMPTY_RESPONSIVE_STATE.formatOverrides}));
 const [masterDialog,setMasterDialog]=useState<MasterDialog>(null);
 const canvasRef=useRef<HTMLDivElement>(null),stageRef=useRef<HTMLDivElement>(null),imageInput=useRef<HTMLInputElement>(null),replaceLayerId=useRef<string|null>(null),startedAt=useRef(0),didMount=useRef(false),undoStack=useRef<EditorSnapshot[]>([]),redoStack=useRef<EditorSnapshot[]>([]),scenesRef=useRef(scenes),responsiveRef=useRef(responsive),clipboardLayer=useRef<SceneLayer|null>(null);
 scenesRef.current=scenes;responsiveRef.current=responsive;const [,setHistoryVersion]=useState(0);
 const checkpoint=()=>{undoStack.current=[...undoStack.current.slice(-49),{scenes:structuredClone(scenesRef.current),responsive:structuredClone(responsiveRef.current)}];redoStack.current=[];setHistoryVersion(v=>v+1)};
 const restore=(snap:EditorSnapshot)=>{setScenes(structuredClone(snap.scenes));setResponsive(structuredClone(snap.responsive));setHistoryVersion(v=>v+1)};
 const undo=()=>{const snap=undoStack.current.pop();if(!snap)return;redoStack.current.push({scenes:structuredClone(scenesRef.current),responsive:structuredClone(responsiveRef.current)});restore(snap)},redo=()=>{const snap=redoStack.current.pop();if(!snap)return;undoStack.current.push({scenes:structuredClone(scenesRef.current),responsive:structuredClone(responsiveRef.current)});restore(snap)};
 const targetFormats=useMemo(()=>mergeCampaignFormats(campaign),[campaign]),scene=scenes.find(s=>s.id===sceneId)??scenes[0],format=formatId==="master"?MASTER_FORMAT:targetFormats.find(f=>f.id===formatId)??MASTER_FORMAT,resolvedLayers=useMemo(()=>resolveSceneLayers(scene,format,responsive),[scene,format,responsive]),sourceLayer=scene?.layers.find(l=>l.id===layerId)??scene?.layers[0],resolvedLayer=resolvedLayers.find(l=>l.id===layerId)??resolvedLayers[0],layer=formatId==="master"?sourceLayer:resolvedLayer?{...resolvedLayer,masterBox:resolvedLayer.box}:sourceLayer,output=useMemo(()=>generateScene(scene,format,responsive),[scene,format,responsive]),totalDuration=useMemo(()=>scenes.reduce((sum,s)=>sum+s.durationMs,0),[scenes]),sceneStart=useMemo(()=>scenes.slice(0,Math.max(0,scenes.findIndex(s=>s.id===scene.id))).reduce((sum,s)=>sum+s.durationMs,0),[scenes,scene.id]),scenePlayMs=playing?Math.max(0,playMs-sceneStart):0,editable=true,canCreate=formatId==="master",inheritance=formatInheritance(format,responsive),activeResponsiveMaster=responsive.responsiveMasters.find(item=>item.sourceFormatId===format.id),tt=campaign&&formatId!=="master"?ttForFormat(campaign,formatId):undefined,ttCheck=useMemo(()=>validateScenesForTT(scenes,tt),[scenes,tt]);
 useEffect(()=>{if(formatId!=="master"&&!targetFormats.some(f=>f.id===formatId))setFormatId("master")},[targetFormats,formatId]);
 useEffect(()=>{if(!campaign?.id)return;if(!didMount.current){didMount.current=true;return}setSaveState("saving");const timer=window.setTimeout(()=>{void api.saveSceneDocument(campaign.id,{...(campaign.creativeDocument||{}),version:2,masterFormat:{width:MASTER_FORMAT.width,height:MASTER_FORMAT.height},scenes,responsiveMasters:responsive.responsiveMasters,formatOverrides:responsive.formatOverrides,updatedAt:new Date().toISOString()}).then(()=>setSaveState("saved")).catch(()=>setSaveState("error"))},850);return()=>window.clearTimeout(timer)},[scenes,responsive,campaign?.id]);
 const patchLayerById=(id:string,patch:Partial<SceneLayer>,record=true)=>{if(record)checkpoint();if(formatId==="master"){setScenes(all=>all.map(s=>s.id===scene.id?{...s,layers:s.layers.map(l=>l.id===id?{...l,...patch}:l)}:s));return}const baseLayer=resolvedLayers.find(item=>item.id===id)??scene.layers.find(item=>item.id===id);if(!baseLayer)return;const override:LayerOverride={...patch};if(patch.masterBox){override.box=patch.masterBox;delete (override as Partial<SceneLayer>).masterBox}setResponsive(current=>setLayerOverride(current,format.id,scene.id,id,baseLayer.role,override))},patchLayer=(patch:Partial<SceneLayer>)=>layer&&patchLayerById(layer.id,patch);
 const addScene=()=>{const n=scenes.length+1,stamp=Date.now(),next:Scene={id:`scene-${stamp}`,name:`Scene ${n}`,durationMs:1800,layers:[{id:`bg-${stamp}`,name:"Background",kind:"shape",role:"background",color:"#151922",masterBox:{x:0,y:0,w:100,h:100},motion:"none",motionDurationMs:0,easing:"linear",startMs:0,endMs:1800,visible:true}]};checkpoint();setScenes(v=>[...v,next]);setSceneId(next.id);setLayerId(next.layers[0].id);setFormatId("master")};
 const addText=(x=12,y=16)=>{checkpoint();const id=`text-${crypto.randomUUID()}`,next:SceneLayer={id,name:"Text",kind:"text",role:"headline",text:"Новый текст",color:"#ffffff",masterBox:{x,y,w:70,h:12},fontSize:30,fontWeight:600,fontFamily:"Inter",motion:"fade",motionDurationMs:360,easing:"ease-out",startMs:0,endMs:scene.durationMs,visible:true};setScenes(all=>all.map(s=>s.id===scene.id?{...s,layers:[...s.layers,next]}:s));setLayerId(id);setTool("select")};
 const addShape=(x=25,y=35)=>{checkpoint();const id=`shape-${crypto.randomUUID()}`,next:SceneLayer={id,name:"Graphic",kind:"shape",role:"graphic",color:"#6c5cff",masterBox:{x,y,w:50,h:28},motion:"scale-in",motionDurationMs:420,easing:"ease-out",startMs:0,endMs:scene.durationMs,visible:true};setScenes(all=>all.map(s=>s.id===scene.id?{...s,layers:[...s.layers,next]}:s));setLayerId(id);setTool("select")};
 const addImageFile=(file?:File,drop?:{x:number;y:number})=>{if(!file)return;const reader=new FileReader();reader.onload=()=>{const assetUrl=String(reader.result),replaceId=replaceLayerId.current;replaceLayerId.current=null;if(replaceId){patchLayerById(replaceId,{kind:"image",role:"hero",assetUrl,fit:"contain",crop:{x:0,y:0,scale:1},name:file.name});setLayerId(replaceId);return}checkpoint();const id=`image-${crypto.randomUUID()}`,next:SceneLayer={id,name:file.name,kind:"image",role:"hero",assetUrl,fit:"contain",crop:{x:0,y:0,scale:1},masterBox:{x:drop?.x??12,y:drop?.y??38,w:76,h:44},motion:"from-right",motionDurationMs:480,easing:"ease-out",startMs:0,endMs:scene.durationMs,visible:true};setScenes(all=>all.map(s=>s.id===scene.id?{...s,layers:[...s.layers,next]}:s));setLayerId(id);setTool("select")};reader.readAsDataURL(file)};
 const requestImage=(id?:string)=>{replaceLayerId.current=id||null;imageInput.current?.click()};
 const pasteLayer=()=>{const source=clipboardLayer.current;if(!source||source.role==="background")return;checkpoint();const id=`${source.kind}-${crypto.randomUUID()}`,copy:SceneLayer={...structuredClone(source),id,name:`${source.name} copy`,masterBox:{...source.masterBox,x:source.masterBox.x+2,y:source.masterBox.y+2}};setScenes(all=>all.map(s=>s.id===scene.id?{...s,layers:[...s.layers,copy]}:s));setLayerId(id);setFormatId("master")};
 const copyLayer=()=>{if(!layer||layer.role==="background")return;const base=scene.layers.find(l=>l.id===layer.id)||layer;clipboardLayer.current=structuredClone(base)};
 const duplicateLayer=()=>{if(!layer||layer.role==="background")return;copyLayer();pasteLayer()};
 const deleteLayer=()=>{if(!layer||layer.role==="background"||scene.layers.length<=1)return;checkpoint();const rest=scene.layers.filter(l=>l.id!==layer.id);setScenes(all=>all.map(s=>s.id===scene.id?{...s,layers:rest}:s));setResponsive(current=>({...current,formatOverrides:current.formatOverrides.map(o=>({...o,sceneOverrides:o.sceneOverrides.map(so=>({...so,layerOverrides:so.layerOverrides.filter(lo=>lo.layerId!==layer.id)}))}))}));setLayerId(rest.find(l=>l.role!=="background")?.id||rest[0]?.id||"")};
 const startCrop=()=>{if(layer?.kind!=="image")return;const crop=layer.crop||{x:0,y:0,scale:1};setCropEdit({id:layer.id,crop:{...crop}});if(!layer.crop)patchLayer({crop})},cancelCrop=()=>{if(cropEdit)patchLayerById(cropEdit.id,{crop:cropEdit.crop});setCropEdit(null)},applyCrop=()=>setCropEdit(null);
 const pointerPercent=(e:ReactPointerEvent)=>{const r=canvasRef.current!.getBoundingClientRect();return{x:(e.clientX-r.left)/r.width*100,y:(e.clientY-r.top)/r.height*100}},onCanvasPointerDown=(e:ReactPointerEvent)=>{if(spacePressed)return;if(!canCreate)return;if(tool==="text"){const p=pointerPercent(e);addText(p.x,p.y)}else if(tool==="shape"){const p=pointerPercent(e);addShape(p.x,p.y)}else if(tool==="image")requestImage()},onDrop=(e:React.DragEvent)=>{if(!editable)return;e.preventDefault();const motion=e.dataTransfer.getData("application/x-bm-motion") as MotionPreset;if(motion&&layer){patchLayer({motion});return}const file=[...e.dataTransfer.files].find(f=>f.type.startsWith("image/"));if(file&&canCreate){const r=canvasRef.current!.getBoundingClientRect();addImageFile(file,{x:(e.clientX-r.left)/r.width*100,y:(e.clientY-r.top)/r.height*100})}};
 const beginPan=(e:ReactPointerEvent)=>{if(!spacePressed||!stageRef.current)return;e.preventDefault();e.stopPropagation();const stage=stageRef.current,sx=e.clientX,sy=e.clientY,left=stage.scrollLeft,top=stage.scrollTop;setIsPanning(true);const move=(p:PointerEvent)=>{stage.scrollLeft=left-(p.clientX-sx);stage.scrollTop=top-(p.clientY-sy)},up=()=>{setIsPanning(false);removeEventListener("pointermove",move);removeEventListener("pointerup",up)};addEventListener("pointermove",move);addEventListener("pointerup",up)};
 const gridSnap=(v:number,a:"x"|"y")=>!snapEnabled?v:Math.round((v/100*(a==="x"?format.width:format.height))/snapPx)*snapPx/(a==="x"?format.width:format.height)*100;
 const snapMove=(rx:number,ry:number,item:SceneLayer)=>{if(!snapEnabled)return{x:rx,y:ry,g:{} as Guides};let x=gridSnap(rx,"x"),y=gridSnap(ry,"y");const g:Guides={},others=scene.layers.filter(l=>l.id!==item.id&&l.visible&&l.role!=="background"),xt=[0,50,100,...others.flatMap(l=>[l.masterBox.x,l.masterBox.x+l.masterBox.w/2,l.masterBox.x+l.masterBox.w])],yt=[0,50,100,...others.flatMap(l=>[l.masterBox.y,l.masterBox.y+l.masterBox.h/2,l.masterBox.y+l.masterBox.h])],ax=[{v:x,o:0},{v:x+item.masterBox.w/2,o:item.masterBox.w/2},{v:x+item.masterBox.w,o:item.masterBox.w}],ay=[{v:y,o:0},{v:y+item.masterBox.h/2,o:item.masterBox.h/2},{v:y+item.masterBox.h,o:item.masterBox.h}];let bx=5*100/format.width,by=5*100/format.height;for(const a of ax)for(const t of xt){const d=Math.abs(a.v-t);if(d<bx){bx=d;x=t-a.o;g.x=t}}for(const a of ay)for(const t of yt){const d=Math.abs(a.v-t);if(d<by){by=d;y=t-a.o;g.y=t}}return{x,y,g}};
 const beginMove=(e:ReactPointerEvent,item:ReturnType<typeof resolveSceneLayers>[number])=>{if(spacePressed||!editable||item.role==="background"||tool!=="select")return;e.stopPropagation();setLayerId(item.id);const r=canvasRef.current!.getBoundingClientRect(),sx=e.clientX,sy=e.clientY;if(cropEdit?.id===item.id&&item.kind==="image"){const c=item.crop||{x:0,y:0,scale:1};const move=(p:PointerEvent)=>patchLayerById(item.id,{crop:{...c,x:c.x+(p.clientX-sx)/r.width*100,y:c.y+(p.clientY-sy)/r.height*100}},false),up=()=>{removeEventListener("pointermove",move);removeEventListener("pointerup",up)};addEventListener("pointermove",move);addEventListener("pointerup",up);return}checkpoint();const start={...item.box},move=(p:PointerEvent)=>{const s=snapMove(start.x+(p.clientX-sx)/r.width*100,start.y+(p.clientY-sy)/r.height*100,{...item,masterBox:start});setGuides(s.g);patchLayerById(item.id,{masterBox:{...start,x:clamp(s.x,-100,200),y:clamp(s.y,-100,200)}},false)},up=()=>{setGuides({});removeEventListener("pointermove",move);removeEventListener("pointerup",up)};addEventListener("pointermove",move);addEventListener("pointerup",up)};
 const beginResize=(e:ReactPointerEvent,item:ReturnType<typeof resolveSceneLayers>[number],h:ResizeHandle)=>{if(spacePressed||!editable||item.role==="background"||cropEdit?.id===item.id)return;e.stopPropagation();checkpoint();const r=canvasRef.current!.getBoundingClientRect(),sx=e.clientX,sy=e.clientY,start={...item.box},aspect=start.w/start.h,move=(p:PointerEvent)=>{const dx=(p.clientX-sx)/r.width*100,dy=(p.clientY-sy)/r.height*100;let{x,y,w,h:hh}=start;if(h.includes("e"))w=start.w+dx;if(h.includes("s"))hh=start.h+dy;if(h.includes("w")){x=start.x+dx;w=start.w-dx}if(h.includes("n")){y=start.y+dy;hh=start.h-dy}w=Math.max(2,w);hh=Math.max(2,hh);if(p.shiftKey&&["nw","ne","se","sw"].includes(h)){if(Math.abs(dx)>Math.abs(dy)){hh=w/aspect;if(h.includes("n"))y=start.y+start.h-hh}else{w=hh*aspect;if(h.includes("w"))x=start.x+start.w-w}}patchLayerById(item.id,{masterBox:{x:gridSnap(x,"x"),y:gridSnap(y,"y"),w:gridSnap(w,"x"),h:gridSnap(hh,"y")}},false)},up=()=>{removeEventListener("pointermove",move);removeEventListener("pointerup",up)};addEventListener("pointermove",move);addEventListener("pointerup",up)};
 useEffect(()=>{if(!playing)return;startedAt.current=performance.now()-playMs;let raf=0;const tick=(now:number)=>{const next=(now-startedAt.current)%totalDuration;setPlayMs(next);let cursor=0;for(const s of scenes){if(next>=cursor&&next<cursor+s.durationMs){setSceneId(s.id);break}cursor+=s.durationMs}raf=requestAnimationFrame(tick)};raf=requestAnimationFrame(tick);return()=>cancelAnimationFrame(raf)},[playing,totalDuration,scenes]);
 useEffect(()=>{const down=(e:KeyboardEvent)=>{const t=e.target as HTMLElement;if(t.matches("input,textarea,select,[contenteditable=true]"))return;if(e.code==="Space"){e.preventDefault();setSpacePressed(true);return}const mod=e.metaKey||e.ctrlKey,k=e.key.toLowerCase();if(mod&&k==="z"){e.preventDefault();e.shiftKey?redo():undo();return}if(mod&&k==="y"){e.preventDefault();redo();return}if(mod&&k==="c"&&layer?.role!=="background"){e.preventDefault();copyLayer();return}if(mod&&k==="v"){e.preventDefault();pasteLayer();return}if(mod&&k==="d"&&layer?.role!=="background"){e.preventDefault();duplicateLayer();return}if(e.key==="Escape"){if(cropEdit)cancelCrop();setTool("select");setEditingTextId(null)}if((e.key==="Backspace"||e.key==="Delete")&&!cropEdit&&layer?.role!=="background"){e.preventDefault();deleteLayer();return}if(cropEdit||!layer||layer.role==="background"||!["ArrowLeft","ArrowRight","ArrowUp","ArrowDown"].includes(e.key))return;e.preventDefault();const px=e.shiftKey?10:1,dx=px/format.width*100,dy=px/format.height*100,b=layer.masterBox;patchLayer({masterBox:{...b,x:b.x+(e.key==="ArrowLeft"?-dx:e.key==="ArrowRight"?dx:0),y:b.y+(e.key==="ArrowUp"?-dy:e.key==="ArrowDown"?dy:0)}})};const up=(e:KeyboardEvent)=>{if(e.code==="Space"){setSpacePressed(false);setIsPanning(false)}};addEventListener("keydown",down);addEventListener("keyup",up);return()=>{removeEventListener("keydown",down);removeEventListener("keyup",up)}},[layer?.id,scene.id,cropEdit,format.width,format.height]);
 const togglePlay=()=>{if(!playing){setPlayMs(sceneStart);startedAt.current=performance.now()-sceneStart}setPlaying(v=>!v)},openCampaignPreview=()=>campaign&&window.open(`/?view=campaign-preview&campaignId=${encodeURIComponent(campaign.id)}`,"_blank","noopener,noreferrer"),saveLabel=saveState==="saving"?"● Saving…":saveState==="error"?"● Save failed":"● Saved",openMasterDialog=(mode:"create"|"update")=>setMasterDialog({mode,family:activeResponsiveMaster?.family||format.family}),affected=masterDialog?affectedFormatsForFamily(masterDialog.family,targetFormats):[],applyMasterDialog=()=>{if(!masterDialog||format.id==="master")return;if(masterDialog.mode==="create")setResponsive(current=>useAsResponsiveMaster(scenes,format,current,masterDialog.family));else if(activeResponsiveMaster)setResponsive(current=>updateResponsiveMaster(scenes,activeResponsiveMaster.id,targetFormats,current));setMasterDialog(null)};
 return <div className="bm-shell"><input ref={imageInput} className="bm-hidden" type="file" accept="image/*" onChange={e=>{addImageFile(e.target.files?.[0]);e.currentTarget.value=""}}/>
 {masterDialog&&<div className="bm-master-dialog-backdrop" onPointerDown={()=>setMasterDialog(null)}><section className="bm-master-dialog" onPointerDown={e=>e.stopPropagation()}><small>RESPONSIVE MASTERS</small><h2>{masterDialog.mode==="create"?"Use as Responsive Master":"Update family"}</h2><p>{masterDialog.mode==="create"?"This format becomes the layout, typography, crop, visibility and motion source for its responsive family.":"The family will be rebuilt from this master. Individual overrides on other formats will be preserved."}</p><label><span>Family</span><select value={masterDialog.family} disabled={masterDialog.mode==="update"} onChange={e=>setMasterDialog({...masterDialog,family:e.target.value as LayoutFamily})}>{Object.entries(RESPONSIVE_FAMILY_LABELS).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><div className="bm-affected-list"><b>{affected.length} affected formats</b><div>{affected.map(item=><span key={item.id}>{item.label}</span>)}</div></div><div className="bm-dialog-actions"><button className="bm-button" onClick={()=>setMasterDialog(null)}>Cancel</button><button className="bm-button primary" onClick={applyMasterDialog}>{masterDialog.mode==="create"?"Use as Responsive Master":"Update family"}</button></div></section></div>}
 <header className="bm-topbar"><div className="bm-brand"><span className="bm-logo">B</span><div><strong>Bannermatic</strong><small>{campaign?`${campaign.name} · Media Plan v${campaign.mediaPlanVersion||0}`:"Campaign / Creative editor"}</small></div></div><div className="bm-top-actions"><button className="bm-icon" title="Undo · ⌘Z" onClick={undo}><Undo2 size={15}/></button><button className="bm-icon" title="Redo · ⇧⌘Z" onClick={redo}><Redo2 size={15}/></button><span className={`bm-save-state ${saveState}`}>{saveLabel}</span>{campaign&&<button className="bm-button" onClick={openCampaignPreview}><Eye size={15}/>All formats</button>}<button className="bm-button" onClick={togglePlay}>{playing?<Pause size={15}/>:<Play size={15}/>}Scene preview</button><button className="bm-button primary" onClick={()=>setGenerated(true)}><Sparkles size={15}/>Adapt to media plan</button></div></header>
 <main className="bm-workspace"><aside className="bm-left bm-panel"><div className="bm-section-head"><div><small>CAMPAIGN</small><h3>Scenes</h3></div><button className="bm-icon" onClick={addScene}><Plus size={15}/></button></div><div className="bm-scenes">{scenes.map((s,i)=><button key={s.id} className={`bm-scene ${s.id===scene.id?"active":""}`} onClick={()=>{setPlaying(false);setSceneId(s.id);setLayerId(s.layers[0]?.id||"")}}><span className="bm-scene-no">{String(i+1).padStart(2,"0")}</span><span><b>{s.name}</b><small>{(s.durationMs/1000).toFixed(1)} sec · {s.layers.length} layers</small></span><ChevronRight size={14}/></button>)}</div><div className="bm-divider"/><div className="bm-section-head"><div><small>SCENE {scenes.indexOf(scene)+1}</small><h3>Layers</h3></div></div><div className="bm-layers">{scene.layers.map(l=><button key={l.id} className={`bm-layer ${l.id===layer?.id?"active":""}`} onClick={()=>setLayerId(l.id)}><span className="bm-layer-icon">{l.kind==="text"?<Type size={13}/>:l.kind==="image"?<ImageIcon size={13}/>:<Layers3 size={13}/>}</span><span><b>{l.name}</b><small>{l.role}</small></span><span onClick={e=>{e.stopPropagation();patchLayerById(l.id,{visible:!l.visible})}}>{l.visible?<Eye size={13}/>:<EyeOff size={13}/>}</span></button>)}</div></aside>
 <section className="bm-center"><div className="bm-stagebar"><div><span className="bm-status-dot"/><b>{format.label}</b><small>{format.width} × {format.height} · 100%</small></div><div className="bm-stage-controls">{formatId!=="master"&&(activeResponsiveMaster?<button className="bm-button responsive-action" onClick={()=>openMasterDialog("update")}>Update family</button>:<button className="bm-button responsive-action" onClick={()=>openMasterDialog("create")}>Use as Responsive Master</button>)}{campaign&&formatId!=="master"&&<div className="bm-tt-wrap"><button className={`bm-tt-compact ${ttCheck.status}`} onClick={()=>setTtOpen(v=>!v)}>TT · {ttCheck.status}</button></div>}<button className={`bm-view-control ${snapEnabled?"active":""}`} onClick={()=>setSnapEnabled(v=>!v)}><Magnet size={13}/>Snap</button><label className="bm-grid-size"><Grid3X3 size={13}/><select value={snapPx} onChange={e=>setSnapPx(Number(e.target.value))}>{[1,2,4,8].map(v=><option key={v} value={v}>{v} px</option>)}</select></label><select value={formatId} onChange={e=>{setFormatId(e.target.value);setTtOpen(false);setTool("select")}}><option value="master">Original Master · 300×600</option>{targetFormats.map(f=><option key={f.id} value={f.id}>{f.label}</option>)}</select></div></div>
 <div ref={stageRef} className={`bm-stage ${spacePressed?"space-pan":""} ${isPanning?"is-panning":""}`} onPointerDown={beginPan} onDragOver={e=>e.preventDefault()} onDrop={onDrop}><div className="bm-tools"><button className={tool==="select"?"active":""} onClick={()=>setTool("select")}><MousePointer2 size={16}/></button><button disabled={!canCreate} className={tool==="text"?"active":""} onClick={()=>setTool("text")}><Type size={16}/></button><button disabled={!canCreate} className={tool==="image"?"active":""} onClick={()=>{setTool("image");requestImage()}}><ImageIcon size={16}/></button><button disabled={!canCreate} className={tool==="shape"?"active":""} onClick={()=>setTool("shape")}><RectangleHorizontal size={16}/></button></div><div className="bm-canvas-frame"><div ref={canvasRef} className={`bm-canvas family-${format.family} editable`} style={{width:`${format.width}px`,height:`${format.height}px`,aspectRatio:"auto"}} onPointerDown={onCanvasPointerDown}><div className="bm-artboard-surface"/>{borderOn&&<i className="bm-banner-border" style={{boxShadow:`inset 0 0 0 1px ${borderColor}`}}/>}{guides.x!==undefined&&<i className="bm-guide vertical" style={{left:`${guides.x}%`}}/>}{guides.y!==undefined&&<i className="bm-guide horizontal" style={{top:`${guides.y}%`}}/>}{output.layers.filter(item=>playing?scenePlayMs>=item.startMs&&scenePlayMs<=item.endMs:true).map(item=><div key={`${scene.id}-${item.id}-${playing?"play":"still"}`} className={`bm-object kind-${item.kind} role-${item.role} motion-${item.motion} ${item.id===layer?.id?"selected":""} ${cropEdit?.id===item.id?"crop-editing":""} ${playing?"is-playing":""}`} style={{left:`${item.box.x}%`,top:`${item.box.y}%`,width:`${item.box.w}%`,height:`${item.box.h}%`,color:item.color,background:item.kind==="shape"?item.color:undefined,fontSize:item.fontSize?`${item.fontSize}px`:undefined,fontWeight:item.fontWeight,fontFamily:item.fontFamily||"Inter","--mx":`${item.motionVector.x}%`,"--my":`${item.motionVector.y}%`,"--motion-duration":`${item.motionDurationMs}ms`} as React.CSSProperties} onPointerDown={e=>beginMove(e,item)} onDoubleClick={e=>{e.stopPropagation();if(item.kind==="text")setEditingTextId(item.id);else if(item.kind==="image"){setLayerId(item.id);startCrop()}}}>{item.kind==="image"&&item.assetUrl?<img src={item.assetUrl} draggable={false} style={{objectFit:item.fit||"contain",transform:`translate(${item.crop?.x||0}%,${item.crop?.y||0}%) scale(${item.crop?.scale||1})`,transformOrigin:"center"}}/>:item.kind==="text"?<span contentEditable={editingTextId===item.id} suppressContentEditableWarning onPointerDown={e=>{if(editingTextId===item.id)e.stopPropagation()}} onInput={e=>{if(editingTextId!==item.id)return;patchLayerById(item.id,{text:e.currentTarget.textContent||""},false)}} onBlur={e=>{patchLayerById(item.id,{text:e.currentTarget.textContent||""},false);setEditingTextId(null)}}>{item.text}</span>:item.role!=="background"?<span className="bm-shape-label">{item.name}</span>:null}{item.id===layer?.id&&item.role!=="background"&&!playing&&!cropEdit&&(["nw","n","ne","e","se","s","sw","w"] as ResizeHandle[]).map(h=><i key={h} className={`bm-handle ${h}`} onPointerDown={e=>beginResize(e,item,h)}/>)}</div>)}</div></div><div className="bm-stage-hint">100% · {format.width}×{format.height} · Hold Space + drag to pan</div></div>
 <div className="bm-timeline"><div className="bm-timeline-head"><div><button className="bm-icon" onClick={togglePlay}>{playing?<Pause size={14}/>:<Play size={14}/>}</button><b>Scene timeline</b><small>Visibility only · motion lives in presets</small></div><span>{(scene.durationMs/1000).toFixed(1)} sec</span></div><div className="bm-scene-ruler">{scenes.map(s=><button key={s.id} className={s.id===scene.id?"active":""} style={{width:`${s.durationMs/totalDuration*100}%`}} onClick={()=>{setSceneId(s.id);setLayerId(s.layers[0]?.id||"")}}>{s.name}</button>)}</div><div className="bm-track-list">{scene.layers.map(l=><div key={l.id} className={`bm-track-row ${l.id===layer?.id?"active":""}`} onClick={()=>setLayerId(l.id)}><span>{l.name}</span><div className="bm-track"><i style={{left:`${l.startMs/scene.durationMs*100}%`,width:`${Math.max(2,(l.endMs-l.startMs)/scene.durationMs*100)}%`}}/></div><small>{l.motion}</small></div>)}</div></div></section>
 <aside className="bm-right bm-panel"><div className="bm-inspector-tabs"><button className={inspectorTab==="design"?"active":""} onClick={()=>setInspectorTab("design")}>Design</button><button className={inspectorTab==="motion"?"active":""} onClick={()=>setInspectorTab("motion")}>Motion</button></div>{layer&&inspectorTab==="design"&&<div className="bm-inspector"><div className="bm-inspector-title"><div><small>SELECTED</small><h3>{layer.name}</h3></div>{layer.role!=="background"&&<div className="bm-layer-actions"><button className="bm-button" onClick={duplicateLayer}><Copy size={13}/>Duplicate</button><button className="bm-text-danger" onClick={deleteLayer}>Delete</button></div>}</div><label><small>ROLE</small><select value={layer.role} onChange={e=>patchLayer({role:e.target.value as LayerRole})}>{ROLES.map(r=><option key={r}>{r}</option>)}</select></label>{layer.kind==="text"&&<><label><small>TEXT</small><textarea value={layer.text||""} onChange={e=>patchLayer({text:e.target.value})}/></label><label><small>FONT FAMILY</small><select value={layer.fontFamily||"Inter"} onChange={e=>patchLayer({fontFamily:e.target.value})}>{FONTS.map(f=><option key={f}>{f}</option>)}</select></label><div className="bm-two"><label><small>STYLE</small><select value={FONT_STYLES.find(s=>s.weight===layer.fontWeight)?.weight||400} onChange={e=>patchLayer({fontWeight:Number(e.target.value)})}>{FONT_STYLES.map(s=><option key={s.name} value={s.weight}>{s.name}</option>)}</select></label><label><small>SIZE</small><input type="number" min={7} max={180} value={layer.fontSize||16} onChange={e=>patchLayer({fontSize:Number(e.target.value)})}/></label></div></>}{layer.kind==="image"&&<button className="bm-button bm-wide-button" onClick={()=>requestImage(layer.id)}><Upload size={14}/>Replace image</button>}<label><small>COLOR</small><div className="bm-color-field"><input type="color" value={layer.color||"#ffffff"} onChange={e=>patchLayer({color:e.target.value})}/><input value={layer.color||"#ffffff"} onChange={e=>patchLayer({color:e.target.value})}/></div></label><div><small>GEOMETRY</small><div className="bm-four">{(["x","y","w","h"] as const).map(k=><label key={k}><span>{k.toUpperCase()}</span><input type="number" step="0.1" value={Number(layer.masterBox[k].toFixed(1))} onChange={e=>patchLayer({masterBox:{...layer.masterBox,[k]:Number(e.target.value)}} as Partial<SceneLayer>)}/></label>)}</div></div></div>}{layer&&inspectorTab==="motion"&&<div className="bm-motion-panel"><div><small>MOTION PRESET</small><h3>{layer.motion}</h3></div><div className="bm-preset-grid">{PRESETS.map(p=><button key={p.id} draggable className={layer.motion===p.id?"active":""} onClick={()=>patchLayer({motion:p.id})} onDragStart={e=>e.dataTransfer.setData("application/x-bm-motion",p.id)}><span>{p.glyph}</span><b>{p.label}</b></button>)}</div></div>}</aside></main>
 {generated&&<div className="bm-format-drawer"><div className="bm-drawer-head"><div><small>CAMPAIGN OUTPUT</small><h2>Adapt to media plan</h2><p>{targetFormats.length} formats</p></div><button className="bm-button" onClick={()=>setGenerated(false)}>Close</button></div><div className="bm-format-grid">{targetFormats.map(f=><button key={f.id} onClick={()=>{setFormatId(f.id);setGenerated(false)}}><div className="bm-thumb" style={{aspectRatio:`${f.width}/${f.height}`}}><span>{f.width}×{f.height}</span></div><b>{f.label}</b></button>)}</div></div>}</div>;
}
