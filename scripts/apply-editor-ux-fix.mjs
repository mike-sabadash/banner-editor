import fs from 'node:fs';
const p='src/web-scene/WebSceneEditor.tsx';
let s=fs.readFileSync(p,'utf8');
if(s.includes('ChevronRight,Copy,Crop')&&s.includes('const undo=()=>')&&s.includes('duplicateLayer=()=>'))process.exit(0);
const rep=(a,b)=>{if(!s.includes(a))throw new Error(`missing replacement anchor: ${a.slice(0,80)}`);s=s.replace(a,b)};
rep('import {ChevronRight,Crop,Eye,EyeOff,Grid3X3,Image as ImageIcon,Layers3,Magnet,MousePointer2,Pause,Play,Plus,RectangleHorizontal,Sparkles,Type,Upload} from "lucide-react";', 'import {ChevronRight,Copy,Crop,Eye,EyeOff,Grid3X3,Image as ImageIcon,Layers3,Magnet,MousePointer2,Pause,Play,Plus,Redo2,RectangleHorizontal,Sparkles,Type,Undo2,Upload} from "lucide-react";');
rep('type MasterDialog={mode:"create"|"update";family:LayoutFamily}|null;','type MasterDialog={mode:"create"|"update";family:LayoutFamily}|null;\ntype EditorSnapshot={scenes:Scene[];responsive:ResponsiveState};');
rep('const canvasRef=useRef<HTMLDivElement>(null),imageInput=useRef<HTMLInputElement>(null),replaceLayerId=useRef<string|null>(null),startedAt=useRef(0),didMount=useRef(false);','const canvasRef=useRef<HTMLDivElement>(null),imageInput=useRef<HTMLInputElement>(null),replaceLayerId=useRef<string|null>(null),startedAt=useRef(0),didMount=useRef(false),undoStack=useRef<EditorSnapshot[]>([]),redoStack=useRef<EditorSnapshot[]>([]),scenesRef=useRef(scenes),responsiveRef=useRef(responsive);\n scenesRef.current=scenes;responsiveRef.current=responsive;\n const [,setHistoryVersion]=useState(0);\n const checkpoint=()=>{undoStack.current=[...undoStack.current.slice(-49),{scenes:structuredClone(scenesRef.current),responsive:structuredClone(responsiveRef.current)}];redoStack.current=[];setHistoryVersion(v=>v+1)};\n const restore=(snap:EditorSnapshot)=>{setScenes(structuredClone(snap.scenes));setResponsive(structuredClone(snap.responsive));setHistoryVersion(v=>v+1)};\n const undo=()=>{const snap=undoStack.current.pop();if(!snap)return;redoStack.current.push({scenes:structuredClone(scenesRef.current),responsive:structuredClone(responsiveRef.current)});restore(snap)};\n const redo=()=>{const snap=redoStack.current.pop();if(!snap)return;undoStack.current.push({scenes:structuredClone(scenesRef.current),responsive:structuredClone(responsiveRef.current)});restore(snap)};');
rep('const patchLayerById=(id:string,patch:Partial<SceneLayer>)=>{if(formatId==="master")', 'const patchLayerById=(id:string,patch:Partial<SceneLayer>,record=true)=>{if(record)checkpoint();if(formatId==="master")');
rep('const requestImage=(id?:string)=>{replaceLayerId.current=id||null;imageInput.current?.click()},deleteLayer=()=>{', 'const requestImage=(id?:string)=>{replaceLayerId.current=id||null;imageInput.current?.click()},duplicateLayer=()=>{if(!layer||formatId!=="master"||layer.role==="background")return;checkpoint();const id=`${layer.kind}-${crypto.randomUUID()}`,copy:SceneLayer={...structuredClone(layer),id,name:`${layer.name} copy`,masterBox:{...layer.masterBox,x:clamp(layer.masterBox.x+2,0,98),y:clamp(layer.masterBox.y+2,0,98)}};setScenes(all=>all.map(s=>s.id===scene.id?{...s,layers:[...s.layers,copy]}:s));setLayerId(id)},deleteLayer=()=>{');
rep('useEffect(()=>{const onKey=(e:KeyboardEvent)=>{const t=e.target as HTMLElement;if(t.matches("input,textarea,select,[contenteditable=true]"))return;', 'useEffect(()=>{const onKey=(e:KeyboardEvent)=>{const t=e.target as HTMLElement;if(t.matches("input,textarea,select,[contenteditable=true]"))return;const mod=e.metaKey||e.ctrlKey;if(mod&&e.key.toLowerCase()==="z"){e.preventDefault();e.shiftKey?redo():undo();return}if(mod&&e.key.toLowerCase()==="y"){e.preventDefault();redo();return}if(mod&&e.key.toLowerCase()==="d"&&layer?.role!=="background"){e.preventDefault();duplicateLayer();return}');
rep('<div className="bm-top-actions"><span className={`bm-save-state ${saveState}`}>{saveLabel}</span>', '<div className="bm-top-actions"><button className="bm-icon" title="Undo · ⌘Z" disabled={!undoStack.current.length} onClick={undo}><Undo2 size={15}/></button><button className="bm-icon" title="Redo · ⇧⌘Z" disabled={!redoStack.current.length} onClick={redo}><Redo2 size={15}/></button><span className={`bm-save-state ${saveState}`}>{saveLabel}</span>');
rep('{layer.role!=="background"&&<button className="bm-text-danger" onClick={deleteLayer}>Delete</button>}', '{layer.role!=="background"&&<div className="bm-layer-actions">{formatId==="master"&&<button className="bm-button" onClick={duplicateLayer}><Copy size={13}/>Duplicate</button>}<button className="bm-text-danger" onClick={deleteLayer}>Delete</button></div>}');
rep('<span contentEditable={editingTextId===item.id} suppressContentEditableWarning onBlur={e=>{patchLayerById(item.id,{text:e.currentTarget.textContent||""});setEditingTextId(null)}}>{item.text}</span>', '<span contentEditable={editingTextId===item.id} suppressContentEditableWarning onPointerDown={e=>{if(editingTextId===item.id)e.stopPropagation()}} onInput={e=>{if(editingTextId!==item.id)return;const el=e.currentTarget;patchLayerById(item.id,{text:el.textContent||""},false);requestAnimationFrame(()=>{const canvas=canvasRef.current;if(!canvas)return;const h=Math.max(1,el.scrollHeight/canvas.getBoundingClientRect().height*100);patchLayerById(item.id,{masterBox:{...item.box,h}},false)})}} onBlur={e=>{patchLayerById(item.id,{text:e.currentTarget.textContent||""},false);setEditingTextId(null)}}>{item.text}</span>');
fs.writeFileSync(p,s);
